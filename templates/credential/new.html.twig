{% extends 'base.html.twig' %}

{% block title %}New Credentials{% endblock %}

{% block body %}
    <div class="card shadow">
        <div class="card-body">
            <a href="{{ path('account') }}" class="btn btn-primary mt-3 rounded-circle shadow">
                <span class="fa fa-arrow-left"></span>
            </a>
            <h1 class="card-title">Create new Credentials</h1>

            {{ include('credential/_form.html.twig') }}
        </div>
    </div>

{% endblock %}
