{% extends 'base.html.twig' %}

{% block title %}New Credentials{% endblock %}

{% block body %}
    <div class="card shadow">
        <div class="card-body">
            <a href="{{ path('credential_index') }}" class="btn btn-primary my-3 rounded-circle shadow">
                <span class="fa fa-arrow-left"></span>
            </a>
            <h1 class="card-title">Create new Credential</h1>

            {{ include('credential/_form.html.twig') }}
        </div>
    </div>

{% endblock %}
